# The complete Unity Linux Dedicated Server build is version-controlled through Git LFS.
# The working tree contains the real files; Git history stores LFS pointers.
game-server/LinuxGameServer/** filter=lfs diff=lfs merge=lfs -text
